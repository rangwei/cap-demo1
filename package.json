{
  "name": "my-bookshop5",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "ISC",
  "version": "1.0.0",
  "engines": {
    "node": "^8.9"
  },
  "files": [],
  "dependencies": {
    "@sap/cds": "^3.21.2",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "sqlite3": "^4.1.1"
  },
  "scripts": {
    "start": "cds run",
    "build": "cds build/all --clean",
    "deploy": "cds deploy"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db/",
          "srv/"
        ],
        "credentials": {
          "database": "my.db"
        }
      }
    }
  }
}